    int maxVal(vector<int> &arr) {
        int n =arr.size();
        for (int i=0;i<n;i++){
            arr[i]-=i;
        }
        
        sort(arr.begin(),arr.end());
        return arr[n-1]-arr[0];
        
        
        
    }
